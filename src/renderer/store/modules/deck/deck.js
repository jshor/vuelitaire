// import Vue from 'vue'

const createState = () => ({
})

const state = createState()

const getters = {
}

const actions = {
}

const mutations = {
}

export default {
  createState,
  getters,
  actions,
  state,
  mutations
}
